/**
 *  希尔排序
 */

// 步长为len >> 2^k的希尔排序
void shellsort(int arr[], int len)
{
    for (int step = len >> 1; step > 1; step >>= 1) {
        for (int i = step;  i < len; ++i) {

        }
    }
}